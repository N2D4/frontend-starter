import Button from "@mui/joy/Button";
export const metadata = {
    title: "Using MDX",
};


# {metadata.title}

Support for MDX is built-in. You can use it to build React components and pages with Markdown. For an example, read this page's source code in `src/app/(main)/using-mdx/page.mdx`.

## Basics \{#basics}
*Italic*, **bold**, ~strikethrough~.

## Headings \{#headings}
### h3 \{#h3}
#### h4 \{#h4}
##### h5 \{#h5}
###### h6 \{#h6}

IDs are also supported (and will be picked up by the [PageOverview](/components/page-overview) component)

## Links \{#links}
Custom link: [Example](https://www.example.com)

Autolink: www.example.com, https://example.com, and contact@example.com.

## Images \{#images}
![Logo image](/logo.png)

You can also scale the image (recommended as this reduces [cumulative layout shifts](https://web.dev/articles/cls)):

![Logo image|x150](/logo.png) ![Logo image|150x75](/logo.png)

## Custom components \{#custom-components}
<Button color="primary" variant="outlined">This is a Joy UI button!</Button>

## Quotes \{#quotes}
> This is a blockquote.

## Footnote \{#footnote}

A note[^1]

[^1]: Big note.

## Table \{#table}

| a | b  |  c |  d  |
| - | :- | -: | :-: |
| This | is | a |  table  |
| demonstrating | various | alignment | options. |

## Lists \{#lists}

- You can make
- normal lists
    - And indent them like this

1. Or you can make
2. numbered lists
    1. And indent them like this

- [x] Or you can make
- [ ] task lists
    - [ ] Again, indented like this
- [ ] Update website www.example.com

## Code \{#code}
Use single backticks for `inline code`.

```
Normal code blocks
```

```tsx
const code = 'syntax highlighting code blocks';
```

```#tsx
const code = 'code block with line numbers';
if (code) {
    return "and multiple lines, too";
}
```

## Dividers \{#dividers}

Horizontal dividers

---

## Math \{#math}

You can write math like $$\sqrt{a^2 + b^2}$$

Or you can do $123$
