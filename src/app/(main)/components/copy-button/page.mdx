import { CopyStaticStringExample } from "./examples";

export const metadata = {
  title: "CopyButton",
};


# {metadata.title}

A button to copy a string to the clipboard.

## Props \{#props}

The same as [`AsyncIconButton`](/components/async-icon-button#props), as well as:

- `content`: The string to copy to the clipboard when clicked.

## Examples \{#examples}

### Copy static string \{#copy-static-string}

<CopyStaticStringExample />
