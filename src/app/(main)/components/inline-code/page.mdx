import { CodeWithinAParagraphExample } from "./examples";

export const metadata = {
  title: "InlineCode",
};


# {metadata.title}

Formats an inline piece of text as monospace and lets the user copy it by clicking (as long as it has no clickable parent).

## Props \{#props}

The same as Joy UI's [Typography](https://mui.com/joy-ui/react-typography/) component.

## Examples \{#examples}

### Code within a paragraph \{#code-within-a-paragraph}

<CodeWithinAParagraphExample />
