import { WaitABitExample } from "./examples";

export const metadata = {
  title: "AsyncButton",
};


# {metadata.title}

A button that is automatically disabled after being clicked, until the async action is complete.

## Props \{#props}

The same as Joy UI's [Button](https://mui.com/joy-ui/react-button/) component, except for the following:

- `onClick` can be an async function.

## Examples \{#examples}

<WaitABitExample />

## See also \{#see-also}

- [Joy UI Button](https://mui.com/joy-ui/react-button/)
- [AsyncIconButton](/components/async-icon-button)
